Subject: Your pledge - '<?=$values['title']?>' at PledgeBank.com.

Unfortunately, your pledge did not meet its target in the required time. You
required <?=$values['target']?> <?=$values['type']?>, but achieved only
<?=$values['actual']?>.

The pledge read: '<?=$values['sentence_first']?>'

The link below will enable you to write to all your signers. Ask them what
they'd like to do next, whether they'd like to go through with the pledge
anyway, or whether they'd like to create a new pledge.

<?=$values['url']?>

Go on get started - good luck!!

<?=$values['signature']?>
